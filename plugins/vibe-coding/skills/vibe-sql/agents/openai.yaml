interface:
  display_name: "Vibe · SQL & connections"
  short_description: "SQL queries, migrations, locks and connection pools"
  default_prompt: "Use $vibe-sql to audit the selected sql & connections boundary."
