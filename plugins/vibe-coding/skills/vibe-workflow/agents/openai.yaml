interface:
  display_name: "Vibe · Workflow checks & maintenance"
  short_description: "Evidence checks, map updates and plugin maintenance"
  default_prompt: "Use $vibe-workflow to audit the selected workflow checks & maintenance boundary."
